# Taste
- Writes in Hinglish (informal Hindi-English mix, e.g. addressing the agent as "bhai") and is comfortable with replies in the same Hinglish register. Confidence: 0.75
- Dislikes "basic and simple" UI; wants product surfaces to read as detailed and premium, and explicitly invites a full rework or upgrade rather than a minimal look. Confidence: 0.55
- Values design-system consistency across the entire project: after a theme/style change, expects every page and surface (including legal, admin tabs, and edge routes) to follow the one new theme with no leftover old-theme classes or un-converted surfaces. Confidence: 0.75
- Wants to talk the approach through before any code is written: explicitly asks for a plan/discussion round first ("let's discuss first before implementing") and invites follow-up questions rather than silent assumptions. Confidence: 0.8
- Expects the feasibility of a proposed change to be validated with real numbers (cost/margin math from actual usage data) before committing to it, not just accepted as a stated goal. Confidence: 0.65
- Prefers operational knobs — limits, concurrency, quotas, pricing — kept in config rather than hardcoded, so they can be tuned or raised later without a code change. Confidence: 0.6
- Before adopting an external technology (article, paper, open-source project), expects deep research into its actual methodology and the competitor landscape — not a restatement of the source's marketing claims. Confidence: 0.6
- Frames new tooling as a fit question ("see if we can use this") — wants the idea ported into their own existing system rather than adopted wholesale, and expects an honest read on whether it is worth doing. Confidence: 0.5
- Once a multi-stage plan is approved, prefers the whole thing implemented in one pass ("all phases") rather than stage-gated delivery with approval checkpoints between stages. Confidence: 0.55
- Wants safeguards enforced inside their own stack before work reaches an upstream provider — e.g. gating unrecognised/non-agent traffic at their own layer rather than paying upstream for it and trusting clients to behave. Confidence: 0.6
- Prefers rules and allowlists grounded in observed real-world data (check what actual clients/SDKs send) instead of plausible guesses. Confidence: 0.6
- When new infrastructure or configuration is introduced, expects explicit operating instructions — where each key/config value belongs, and the silent failure modes — not just working code; will ask directly for a per-account list of missing keys and exactly where each one goes. Confidence: 0.7
- When a second symptom is reported, asks whether it is the same bug class as one already diagnosed ("usme koi issue tha kya similar") — expects the shared root cause to be hunted across the codebase, not each report patched in isolation. Confidence: 0.5
- Prioritises confident product positioning in marketing copy (e.g. marketing a model as "our own") over maximum technical disclosure, provided the claim stays defensible. Confidence: 0.5
- Keeps secrets in a local keys file alongside `.env` (with the provider documentation links in it) and expects the agent to read credentials from there instead of having keys pasted into the chat transcript. Confidence: 0.55
- Validates locally before production: explicitly chooses to run and test on the local stack first and defers environment/deploy work until local verification passes. Confidence: 0.6
- For their own products, wants an internal all-access "super account" that bypasses every plan limit and is not exposed publicly — used both for personal use and as an incidental load test. Confidence: 0.5
- Prefers to exercise the product hands-on rather than have the agent test for them: expects to be handed a runnable base URL and key (e.g. via a started tunnel like ngrok) so they can drive the testing themselves. Confidence: 0.55
- After shipping, expects continuous monitoring of real traffic for regressions plus deliberate collection of usage/routing data to feed future improvements (e.g. smart-router learning) — not just fixing whatever is reported. This extends to the user's own ongoing project runs: they announce that multiple projects are now live and ask the agent to keep watching for bugs while also harvesting router data. Confidence: 0.72
- When the agent surfaces a deliberate design tradeoff and asks for a decision, resolves it by having the missing logic actually written (e.g. "write the de-escalation logic, it's important") rather than leaving the gap open as a known flag. Confidence: 0.55
- Prefers the fully-enabled path over the conservative baseline when choosing between options — e.g. turning the skill router live for real decisions and disagreement data instead of continuing cost-only until more data accumulates. Confidence: 0.5
- Gives go-ahead decisions tersely ("haan bhai, ... kar dete hain") and expects the agent to then proceed with implementation without further confirmation rounds. Confidence: 0.5
- Wants usage/telemetry reported as concrete tabular breakdowns (e.g. request counts per endpoint model × upstream provider), not prose summaries, and expects them on demand for an arbitrary time window (e.g. "last 2 hours") as well as on the recurring schedule. Confidence: 0.6
- Expects status reports pushed on a recurring schedule (e.g. a summary every 10 minutes) while work is running, rather than reporting only when asked. Confidence: 0.6
- When several workloads/projects are running in parallel, expects the agent to proactively sweep the logs for errors ("koi error to nahi") instead of waiting for a failure to surface. Confidence: 0.55
- Expects confirmation that shipped optimisations actually fire in production — asks whether the techniques they added (e.g. comparison, context compression) are triggering at all, not just whether the code was merged. Confidence: 0.6
- Expects upstream model identifiers (exact model/version names) to be confirmed against the provider's own model endpoint/API before being wired into routing, rather than assumed from docs or past config. Confidence: 0.6
- Treats purchased-plan parameters (concurrency ceilings, quotas) as values read off the provider's own dashboard and corrects the code whenever the hardcoded number disagrees with the provider's real limit — in either direction: raising it when understated ("agnes pe max 16 concurrent allowed hain, 10 nahi") and lowering it when overstated (e.g. electronhub's real concurrency is 1, not 2). Wants the exact plan capability, neither a conservative nor an optimistic guess. Confidence: 0.6
- Expects hard per-model constraints — notably context-window limits — to be enforced as a routing guard: if a request exceeds a lane's context size (e.g. 200k tokens), it must not be dispatched to that lane at all. Confidence: 0.6
- When an upstream provider rejects requests, steers the investigation to their own integration config first ("check whether we're hitting the wrong base URL") instead of accepting an account/plan-level limitation as the cause. Confidence: 0.65
- Actively hunts for cheaper upstream options — asks whether a provider's model catalogue includes free models, on top of already favoring flat-rate lanes. Confidence: 0.45
- For non-obvious bugs, expects a deep-research/investigation pass into the real root cause before a fix is attempted ("ek baar achche se investigate kar lo", "deep research kar lo kese resolve kar sakte hain") — a plausible quick patch is not enough. Confidence: 0.6
- Treats the still-unresolved issues the agent flags at the end of a fix as important follow-up work to be investigated next, not as acceptable known limitations to leave open. Confidence: 0.55
- Separates "fix now" from "tune later": lands a concrete root-cause infrastructure fix immediately (e.g. adding a per-provider concurrency cap) while explicitly deferring data-dependent work (e.g. router calibration) until the parallel projects finish. Confidence: 0.5
- Wants idle free/flat-rate capacity actually used rather than held back for special cases: when a lane bills nothing at the margin, directs that routine traffic be leaked onto it in a randomised "1-in-N"-style share instead of reserving it for hard turns only. Confidence: 0.55
- Prefers deleting legacy code paths whose original justification no longer applies (e.g. a hardcoded failover hop kept for an issue that has since changed) over leaving them in place as dead weight — dead code should be removed, not tolerated. Confidence: 0.5
- When a lane cannot serve a class of requests as-is, prefers adapting the request to the provider's requirement (e.g. converting non-stream turns to streaming) over adding a skip/bypass flag — the free capacity should be made usable, not cut off. Confidence: 0.55
- Reviews a shipped fix by probing whether it uses the provider's standard protocol signal (e.g. an HTTP `Retry-After` header) rather than only ad-hoc body parsing, and asks for the behaviour to be made "smarter" — wants the generalised, robust handling over a single-provider special case. Confidence: 0.5
- Expects documentation and marketing copy (endpoint counts, plan names, prices, trial descriptions) to be swept and updated across every surface whenever backend config changes — stale strings like old model names or "three endpoints" must be hunted down with searches, not just fixed where noticed. Confidence: 0.5
- Treats a task as done only after a full verification gate — test suite, typecheck, lint, a production build, plus a live end-to-end smoke check — with each result reported explicitly rather than a bare "done". Confidence: 0.5
- Reports failures by pasting the raw client-facing error verbatim (status line, message, and the trace ID) instead of describing the symptom — expects the trace ID to be used to find the offending request in server logs. Applies to transport-level failures too (e.g. pasting a bare "Connection error." with its trace ID and the boilerplate "type continue to try again" footer), not only HTTP API errors. Confidence: 0.7
- Re-reports the same recurring error verbatim (with a fresh trace ID) in later sessions instead of treating a previous "this isn't our bug" attribution as closed — recurrence of a known error is treated as still needing a real root-cause explanation (e.g. repeatedly pasting the `role 'tool' must be a response to a preceding message with 'tool_calls'` 400, each time with a new trace ID, even after it was already diagnosed as a harness-side issue and after a gateway-side fix was shipped; this has now recurred across multiple separate sessions, each time in a fresh session with a new trace ID, keeping long-running sessions in mind as the affected surface). Confidence: 0.99
- Wants self-inflicted conditions kept out of provider-fault telemetry: when their own admission control (budget/saturation refusal) causes a request to move on, it must be logged as a distinct event rather than counted as a lane failover, so the upstream failure rate stays a readable measure of provider health. Confidence: 0.55
- When issues surface in the projects they are running, expects them cross-referenced against the logs and wants their own optimizations explicitly ruled in/out as the cause of the regression (e.g. suspecting that cutting contexts at 100k is what broke tool calls) before the failures are attributed to the provider. Confidence: 0.6
- Isolates fault by running the same agent workload through more than one path — e.g. their own harness against `openrouter/glm-5.3` and `openrouter/theta` direct, alongside theta through the gateway — and expects each failure category (429s, ghost/no-tool-calls, wall-clock caps, tool-exec errors, trailing artifacts) to be attributed to a specific layer: our gateway, the raw model, or the environment. Confidence: 0.55
- Reports those findings as a compact failure-category matrix (error category × endpoint with severity markers like 🔴/🟢 plus a "Primary Reason" column) and treats every row as a claim for the agent to verify against logs rather than accept — expects a per-row verdict (ours / not ours / confirmed) with evidence attached. Confidence: 0.5
- Expects the routing layer's injected latency to fit inside the client's own wall-clock budget: their harness caps a feature run at ~10 minutes, and throttle/ramp delay that pushes a run past that cap is counted as the gateway's bug, not the model's or an acceptable cost of pacing. Confidence: 0.5
- When the product's global policy conflicts with their own benchmark workload, resolves it with the surgical account-scoped override (put the benchmark key on the internal `super` plan) rather than weakening limits that all users are subject to. Confidence: 0.5
- Questions non-obvious routing/config workarounds and wants their rationale stated before accepting them — e.g. challenges a project pinned to one lane (`free-lane-sticky(electronhub)`) by asking whether there was ever a reason for the pin, and defaults to removing the pin unless a justification is given. Deliberate special-cases must earn their place. Confidence: 0.45
