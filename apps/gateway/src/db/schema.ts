// Gateway imports the unified platform schema (single source: apps/web/src/db/schema.ts).
// User identity lives in better-auth's `user` table (GitHub OAuth).
export * from "../../../web/src/db/schema";